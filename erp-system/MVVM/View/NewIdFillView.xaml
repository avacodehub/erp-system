<UserControl x:Class="erp_system.MVVM.View.NewIdFillView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:erp_system.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Padding="15" Background="White">
        <StackPanel VerticalAlignment="Stretch">
            <StackPanel Orientation="Horizontal" >
                <StackPanel>

                    <TextBlock Text="Id Number" Width="200" />
                    <TextBox Text="{Binding}" Margin="0 5 0 0" />
                </StackPanel>
                <StackPanel>

                    <TextBlock Text="Description" Width="200"/>
                    <TextBox Text="{Binding}" />
                </StackPanel>

            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0 25" >
                <StackPanel>

                    <TextBlock Text="Description" Width="200"/>
                    <TextBox Text="{Binding}" />
                </StackPanel>

            </StackPanel>

            <Button HorizontalAlignment="Right" VerticalAlignment="Bottom">Forward</Button>
        </StackPanel>
    </Border>
</UserControl>
